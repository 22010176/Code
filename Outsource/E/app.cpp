#include <bits/stdc++.h>
#include <windows.h>
using namespace std;

string Reverse(string sourse) {
  string result = "";
  for (int i = sourse.length() - 1; i >= 0;i--) result += sourse[i];
  return result;
}
string Replace(string source, char reset) {

}
int main() {
}