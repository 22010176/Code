#include "../utils/hea.h"
#include "../utils/matrix.c"
int size[2] = { 40,100 }; // 40 x 100
int drawAxix(int* pos, int* range) {

}

char* MakeColor(int x) {

}
int main() {
  int a = 10434;
  int* num = malloc(ceil(log10(a)) * sizeof(int));
  printf("%s*****", color(45));
}